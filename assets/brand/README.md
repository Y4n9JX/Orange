# Branding assets

Put your source icon here, for example:

```text
assets/brand/logo.png
```

Recommended icon: square PNG, 1024×1024, transparent background if needed. The script will crop and generate platform-specific sizes automatically.
Then run:

```bash
python3 scripts/brand.py --name YourApp --icon assets/brand/logo.png
```
